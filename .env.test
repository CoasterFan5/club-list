DBURL="postgresql://prisma:prisma@db/tests"
BETA=true

#s3 stuff
r2endpoint="http://s3.docker.test:8000"
accessKeyId="accessKey1"
accessKeySecret="verySecretKey1"
bucket="clubsaurus" # this will be auto created with docker
mediaurl="http://localhost:8000/clubsaurus" #this is the url of the media server, we recommend using cloudflare r2 and setting up domain access for all image hosting. However, in this case, we just use a public simulated s3 container
